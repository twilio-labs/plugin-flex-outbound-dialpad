{
  "name": "plugin-outbound-dialing-with-conference",
  "description": "Example front end implementation of a dialpad that creates outbound calls via a websocket to a backend service",
  "version": "1.1.0",
  "author": "Jared Hunter <jhunter@twilio.com>",
  "private": true,
  "scripts": {
    "postinstall": "flex-check-start",
    "prestart": "flex-check-start",
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-app-rewired eject"
  },
  "devDependencies": {
    "@twilio/flex-ui": "^1",
    "react-app-rewire-flex-plugin": "^1",
    "react-app-rewired": "^1.6.2"
  },
  "dependencies": {
    "flex-plugin": "1.3.4",
    "react": "16.5.2",
    "react-click-n-hold": "^1.0.7",
    "react-dom": "16.5.2",
    "react-scripts": "1.1.5"
  },
  "config-overrides-path": "node_modules/react-app-rewire-flex-plugin"
}
